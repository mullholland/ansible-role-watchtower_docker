---
- name: Watchtower systemd reload
  ansible.builtin.systemd:
    daemon_reload: yes

- name: Watchtower restart
  ansible.builtin.service:
    name: "watchtower"
    state: restarted
    enabled: true
