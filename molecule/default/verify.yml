---
- name: Verify
  hosts: all
  gather_facts: true
  vars:
    verify:
      package: "example"
      compose_file: "/opt/watchtower/docker-compose.yml"
      compose_line: '    image: "containrrr/watchtower:latest"'
      compose_owner: "homelab"
      compose_group: "homelab"
      compose_mode: "0640"
      service_file: "/etc/systemd/system/watchtower.service"
      service_line: 'Description=watchtower service with docker compose'
      service_owner: "root"
      service_group: "root"
      service_mode: "0770"

  tasks:
    - name: SmokeTests
      ansible.builtin.debug:
        msg:
          - "ansible_version => {{ansible_version}}"
          - "ansible_distribution => {{ ansible_distribution }}"
          - "ansible_distribution_major_version => {{ ansible_distribution_major_version }}"
          - "ansible_os_family  => {{ ansible_os_family}}"
          - "ansible_system  => {{ ansible_system }}"

    - name: check {{ verify.compose_file }} conf file
      lineinfile:
        name: "{{ verify.compose_file }}"
        line: "{{ verify.compose_line }}"
        state: present
        owner: "{{ verify.compose_owner }}"
        group: "{{ verify.compose_group }}"
        mode: "{{ verify.compose_mode }}"
      check_mode: true
      register: molecule_hcl
      failed_when: (molecule_hcl is changed) or (molecule_hcl is failed)

    - name: check {{ verify.service_file }} conf file
      lineinfile:
        name: "{{ verify.service_file }}"
        line: "{{ verify.service_line }}"
        state: present
        owner: "{{ verify.service_owner }}"
        group: "{{ verify.service_group }}"
        mode: "{{ verify.service_mode }}"
      check_mode: true
      register: molecule_hcl
      failed_when: (molecule_hcl is changed) or (molecule_hcl is failed)

    # Whether the container actually stays running is not checked here:
    # nested Docker-in-Docker inside the molecule test instance is fragile
    # for reasons unrelated to this role (storage driver quirks, registry
    # rate limits, etc.). Rendering the compose file/systemd unit correctly
    # and having systemd accept the service start is the meaningful check.
